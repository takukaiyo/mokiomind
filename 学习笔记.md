## RMSNorm
layer norm：减去样本的均值，除以样本的方差，使得整体样本不要太分散。

RMS（root mean square） Norm：去除了减去均值的操作，也就是没有去中心化的操作，只有缩放的操作。RMSnorm就是均值为0的layer norm。

### L-N计算
$$
\bar{a}_i = \frac{a_i - \mu}{\sigma} g_i, \quad y_i = f(\bar{a}_i + b_i)
$$
### L-N均值方差参数
$$
\mu = \frac{1}{n} \sum_{i=1}^n
a_i, \quad \sigma = \sqrt{\frac{1}{n} \sum_{i=1}^n (a_i - \mu)^2 + \epsilon}
$$

### RMSNorm计算
$$
\bar{a}_i = \frac{a_i}{\sigma} g_i,
$$
$$
\sigma = \sqrt{\frac{1}{n} \sum_{i=1}^n a_i^2 + \epsilon}
$$


优点：没有了去中心化的操作，可以提升运行效率。

代码：
```python
import torch
import torch.nn as nn


class RMSNorm(nn.Module):
    def __init__(self, dim, eps=1e-5):
        super(RMSNorm, self).__init__()
        self.weight = nn.Parameter(torch.ones(dim))
        self.eps = eps

    def _norm(self, x):
        return x * torch.rsqrt(x.pow(2).mean(-1, keepdim=True) + self.eps)

    def forward(self, x):
        return self._norm(x.float()) * self.weight.type_as(x)
```

## RoPE (Rotary Position Embedding) 与 YaRN

### 基本概念

Transformer 模型本身不具备感知词元顺序的能力，需要引入位置编码。RoPE 是一种将绝对位置信息通过旋转矩阵与词元的嵌入表示相乘，从而在 Attention 计算中融入相对位置信息的编码方法。**其核心思想是通过旋转来融合绝对位置，最终在注意力点积中体现出相对位置关系**。

YaRN (Yet another RoPE extensioN) 是一种改进方案，旨在无需微调或仅需极少微调的情况下，显著提升基于 RoPE 的模型（如 LLaMA）的**长文本外推能力**。其核心是通过在推理时对 RoPE 的旋转角进行 NTK-aware 的缩放。

### RoPE 计算 (原始版本)

1. **复数视角下的旋转**

对于一个在位置 m的词元嵌入向量，我们将其视为复数。RoPE 通过乘以一个旋转因子$e^{(i mθ)}$来注入位置信息：
$$
f_q(x_m, m) = (W_q x_m) * e^{(i mθ)}
$$
其中，θ 是预设的基础旋转角。

2. 向量形式的计算（更常用）

实践中，我们将 d 维的查询（Query）或键（Key）向量两两分组，视为 d/2 个二维向量。对每个二维向量 $[x_i, x_j]$应用旋转矩阵：

对于位置为 m的向量：
$$
RoPE_{\theta, m}^{(i)} = \begin{bmatrix}
\cos m\theta_i & -\sin m\theta_i \\
\sin m\theta_i & \cos m\theta_i
\end{bmatrix}
$$
推广到整个向量，RoPE 变换相当于将向量与一个分块对角旋转矩阵 $R_Θ^m$相乘。

3. Attention 中的相对性证明

令$q_m$为位置 m 的查询向量，$k_n$为位置 n 的键向量。应用 RoPE 后：
$$
q_m^T k_n = (R_Θ^m W_q x_m)^T (R_Θ^n W_k x_n) = x_m^T W_q^T (R_Θ^{m-n})^T W_k x_n
$$
注意力分数 $q_m^T k_n$仅依赖于相对位置 m-n，这正是我们需要的性质。

基础旋转角 θ_i通常设置为：$θ_i = base^{-2i/d}$, 其中 base是一个很大的常数（如 10000）。

YaRN 的改进

YaRN 发现，直接外推（即在训练长度之外使用更大的位置 m）会导致注意力分数的高频震荡和极端值。其解决方案是在推理时，对 RoPE 中的位置索引 m进行缩放，并调整注意力计算。

核心：NTK-aware 缩放因子

YaRN 将 RoPE 计算中的原始位置 m替换为 m'：

$$m' = m * s$$

其中，缩放因子 s是 NTK-aware 的：

$$s = (scale_factor * L / L_train) ^ {(d / (d-2))}$$

L: 当前推理的上下文长度。

$L_train$: 模型预训练时的上下文长度。

d: 词嵌入的维度。

$scale_factor$: 一个可调的缩放超参数（通常 >1）。

直观理解：这种方式并不是线性地压缩所有频率，而是更激进地压缩高频（对应较小的 i，即 $θ_i$较大），更温和地压缩低频。这能更好地保持模型在训练长度内的性能，同时增强外推能力。

注意力温度调整 (可选)

在应用上述缩放后，YaRN 建议在计算 softmax 前，将注意力分数除以一个温度因子 t（通常 $t = sqrt(s)$），以稳定训练长度内的分布。